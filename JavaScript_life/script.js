// var (variable Declaration) is used to declare a variable in JavaScript.
var name; //decl in same line
name="Pavithra"; //initialize in same line
console.log(name); 

var name; //re-declaration is allowed 
name="pavi" //re-initialization is allowed
console.log(name);
